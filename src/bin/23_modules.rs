// ------------------------------------------------------
//     - Rust Module
//          - Binary crates: can execute
//          - Library crate: can be used as module for other crate (binary)
// ------------------------------------------------------
fn main() {
    /*
        file is individual crate
        modules are also the individual file
            but modules are lower in hierarchy than crates
            individual crate can include multiple modules
                ex: lib.rs
                    pub mod file_1;
                    pub mod file_2;
    */
}
